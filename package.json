{
  "name": "present-drop",
  "version": "1.0.0",
  "author": "Euan Goddard",
  "scripts": {
    "dev": "vite --config vite/config.dev.mjs",
    "build": "vite build --config vite/config.prod.mjs",
    "deploy": "npm run build && gh-pages -d dist --cname=present-drop.euans.space"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "terser": "^5.31.6",
    "typescript": "^5.5.4",
    "vite": "^5.4.3"
  },
  "dependencies": {
    "phaser": "^3.85.1"
  }
}
